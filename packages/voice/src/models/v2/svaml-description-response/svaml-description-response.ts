/**
 * Response containing a human-readable description of the SVAML call flow.
 */
export interface SvamlDescriptionResponse {
  /** Human-readable description of the call flow. */
  description?: string;
}
